LIST OF BUGS (in order of high -> low priority)
-----------------------------------------------
* classes
 > sections with list attributes (e.g. contours) will crash program when trying to iter through NoneType. Change None -> [] upon __init__

* handleXML
 > No QApplication for showing overwrite conflicts if run outside of pyreconGUI

* curationTool:
 > need some way to see if its working or not
 	idea: set button to flat when clicked, unflat when series loaded

* excelTool:
 > problems when writing over excel workbook with same name